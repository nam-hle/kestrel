/**
 * kestrel MCP server adapter. Keeps @kestrel/core warm across tool calls,
 * translates MCP tool requests <-> core calls. No analysis logic.
 * See docs/DESIGN.md Section 1.
 *
 * Skeleton: entry only. MCP wiring TBD.
 */
export function main(): void {
	throw new Error("not implemented");
}
