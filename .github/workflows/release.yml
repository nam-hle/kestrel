name: Release

on:
  push:
    branches:
      - main
  # Manual escape hatch: publish every package on demand. Needed when a release was
  # tagged but never published (e.g. action-pushed tags don't trigger workflows).
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # On push to main: maintain a release PR (version bumps + CHANGELOGs) from the
  # Conventional Commits. Merging it tags the release (e.g. `core/v0.3.0`) and the
  # publish job below runs in the same workflow run when a release was created.
  release-please:
    name: Release PR
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      pull-requests: write
    outputs:
      # On a release: the paths release-please actually cut. On manual dispatch: every
      # package, so the escape hatch republishes the whole workspace.
      should_publish: ${{ steps.release.outputs.releases_created == 'true' || github.event_name == 'workflow_dispatch' }}
      paths: ${{ steps.paths.outputs.paths }}
    steps:
      - uses: googleapis/release-please-action@5c625bfb5d1ff62eadeeb3772007f7f66fdcf071 # v4
        id: release
        with:
          # A fine-grained PAT (RELEASE_PLEASE_TOKEN) so the release PRs it opens trigger
          # CI normally — GITHUB_TOKEN-created PRs don't, which forces an admin-merge.
          # Falls back to GITHUB_TOKEN when the secret is absent.
          token: ${{ secrets.RELEASE_PLEASE_TOKEN || secrets.GITHUB_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      - name: Resolve packages to publish
        id: paths
        env:
          RELEASED: ${{ steps.release.outputs.paths_released }}
          EVENT: ${{ github.event_name }}
        run: |
          if [ "$EVENT" = "workflow_dispatch" ]; then
            echo 'paths=["packages/core","packages/cli","packages/mcp"]' >> "$GITHUB_OUTPUT"
          else
            echo "paths=${RELEASED:-[]}" >> "$GITHUB_OUTPUT"
          fi

  # When release-please cut a release this run, publish each released package to
  # npm via OIDC trusted publishing (no token — keyless, with provenance). Driven
  # by the release-please outputs, not a tag push: tags pushed by the action don't
  # re-trigger workflows, so a tag-triggered job would never fire.
  publish:
    name: Publish
    needs: release-please
    if: needs.release-please.outputs.should_publish == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write # OIDC trusted publishing + provenance
    strategy:
      # Don't let one package's failure (e.g. a version already on npm) cancel the
      # others — each publish is independent.
      fail-fast: false
      matrix:
        path: ${{ fromJSON(needs.release-please.outputs.paths) }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: ./.github/actions/setup

      - name: Build
        run: nadle build

      - name: Setup Node.js for trusted publishing
        uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903 # v6.0.0
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      # pnpm publish rewrites the `workspace:*` protocol to the real version in the
      # published tarball; plain `npm publish` ships the literal string and breaks installs.
      - name: Publish to npm
        run: pnpm publish --provenance --access public --no-git-checks
        working-directory: ${{ matrix.path }}
